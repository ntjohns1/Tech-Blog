{{!-- --}}
    {{#each posts as |post|}}
    <a href="/edit/{{post.id}}">
        <div class="btn flex-row justify-space-between">
            <p class="card-body">{{post.title}}</p>
            <p class="card-body">{{post.formattedDate}}</p>
        </div>
    </a>
    {{/each}}