    {{#each posts as |post|}}
<div class="card">
    <a href="/single-post">
        <h2 class="card-header">
            {{post.title}}
        </h2>
    </a>

    <div class="card-body">
        <p>
            {{post.body}}
        <p>
    </div>
</div>
    {{/each}}